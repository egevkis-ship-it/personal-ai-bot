# Бриф WORKOUT2 — 6 находок из живого теста (по одному коммиту на блок, W2-1 → W2-6)

Правила: логика в `app/`/`api`, веб тонкий, owner-scoped, `py_compile`+`node --check`,
sw +1 при фронте, РЕАЛЬНАЯ проверка в браузере, push → деплой `bovve9287cs7jjhkteh2bdfk`
→ `/healthz` ok. Итог в секцию после каждого блока.

## W2-1 — ввод подходов: аккордеон вместо «шторки на шторке»
Тап по упражнению в активной тренировке (`renderActive`) разворачивает/сворачивает
подходы инлайн прямо под строкой: список введённых подходов (правка/удаление) +
мультиввод WK-2 (строки «вес × повторы», «➕ ещё», «✓ Сохранить», голос/текст) — всё
ВНУТРИ раскрытой секции, без `sheet()`. Повторный тап / тап по другому упражнению
сворачивает. Переиспользовать `_setRows/renderSetRows/confirmSets/setLabel/editSet/rmSet`,
сохранить поведение таймера отдыха.

## W2-2 — поле веса заменяет, а не дописывает
`.srin` (web/app.js:361-375) предзаполнены, ввод дописывается. Добавить
`onfocus="this.select()"` на все числовые поля подходов (вес/повт/мин/сек) и в `stepRow`.

## W2-3 — миниатюры в пикере крупнее
`.ex-thumb` ~46→~64, текст строки чуть крупнее, не ломая скролл.

## W2-4 — пиктограммы крупнее в остальных меню
WK-3 увеличил только `.ex-row`; увеличить `.ic` в хабе/Замеры/История/Настройки/
расписании согласованно.

## W2-5 — «кг» у веса везде
Строка «цель» (web/app.js:235) — вес без единицы; добавить «кг» там и везде, где вес
голой цифрой.

## W2-6 — главная зовёт начать выполненную (баг)
Дашборд (api/main.py:802) берёт `today_plan` без проверки завершённой за сегодня.
Скрывать `today_plan` (или «✅ выполнено»), если есть `workouts` с `finished_at` за
сегодня — `EXISTS` как в `/workouts/week` (main.py:1227), по `planned_workout_id` и/или дате.

## W2-7 — Выбор упражнения в пикере: добавлять в тренировку, а НЕ открывать шторку подходов
Находка владельца (живой тест): при добавлении упражнения в свободную/активную тренировку
тап по упражнению в пикере СРАЗУ открывает шторку «Подходы» (`openAddSet`), а аккордеон
(W2-1) не задействован. Логичнее: тап по упражнению просто **добавляет его строкой в
тренировку**, а подходы пользователь вписывает уже ВНУТРИ тренировки через аккордеон.
- Причина: `chooseEx(wid,…)` для активной тренировки (`web/app.js:625-636`, ветка `wid!=0`)
  вызывает `openAddSet(wid, null, {…})` (шторка). Заменить: добавить упражнение в
  `window._WO.exercises` (с `sets:[]`, выбранный `type`), закрыть пикер (`closeSheet`),
  `renderActive`, и **авто-развернуть аккордеон именно этого нового упражнения** (`activeExpanded`
  = его индекс) — чтобы подходы вводились инлайн, без шторки.
- Никаких шторок в этом сценарии. 0-подходное упражнение живёт в локальном состоянии
  тренировки до первого сохранённого подхода (как и сейчас при добавлении) — это ок.
- Можно дать возможность добавить несколько упражнений подряд (пикер закрывается, тап
  «Добавить упражнение» снова открывает) — на усмотрение; главное убрать авто-шторку подходов.
- **Не трогать** ветку архив-композера (`wid===0 && window._arch`) — там свой draft-поток,
  он работает. Плановый/шаблонный пикеры (target-шторка веса/повторов плана) — тоже не про
  это, не трогать.
- Acceptance: в активной тренировке тап по упражнению в пикере добавляет его строкой и
  раскрывает аккордеон этого упражнения для ввода подходов; шторка «Подходы» при добавлении
  не появляется; ввод/сохранение подходов идёт через аккордеон (W2-1).

## Ревью 2026-06-30 08:31 UTC (проверка завершения)
Новых коммитов нет (HEAD `858771a`), прошлый прогон чистый, clean_runs=37→38. Прод `https://app.egorkis.com/healthz` → `{"ok":true,"env":"production"}` ✅. Новых P0/P1 нет.

**НЕ отключаюсь — блок W2 открыт.** Файл AGENT_TASKS_WORKOUT2.md — это бриф (W2-1..W2-6), но НИ ОДИН пункт не реализован: нет коммитов с тегом `W2-` в git-логе, нет ни одной секции «## Итог»/«## Ревью ✅» по W2. Поскольку W2-1..6 явно входят в перечень п.8 авто-отключения и они НЕ закрыты, условие завершения не выполнено независимо от ситуации с 4_COACH.

**Владельцу:** очередь W2 (аккордеон подходов, select() на ввод веса, крупные миниатюры/иконки, «кг» у веса, главная не зовёт начать выполненную) ещё не взята в работу разработчиком — жду коммитов. Параллельно остаётся прежний вопрос по флагману 4_COACH (код-след есть, секции «## Ревью» нет).

## Итог W2-1 (Code, 2026-06-30, коммит `5dae3eb`)
Тап по упражнению в активной тренировке разворачивает подходы инлайн под строкой
(аккордеон): список подходов (✏️/🗑) + WK-2 мультиввод (строки вес×повт, «➕ ещё»,
«✓ Сохранить», голос/текст) — без `sheet()`. Повторный тап / другое упражнение
сворачивает (один открыт за раз; состояние переживает ре-рендеры сохранения, сбрасывается
при смене тренировки). Рефактор: `initSetEntry()`+`setEntryHtml()` вынесены из `openAddSet`
(аккордеон и шторка архив-композера делят один WK-2-ввод); confirmSets/editSet/rmSet/
renderSetRows переиспользованы; таймер отдыха цел. Удалён мёртвый `openExercise`. CSS
`.acc-body`. Фронт-онли, sw **v36→v37**. Проверка в браузере: раскрытие показывает
подходы + префилл-ввод; сохранение оставляет аккордеон открытым и добавляет подход;
второй тап сворачивает. pytest 143. Acceptance W2-1 ✅.

## Итог W2-2 (Code, 2026-06-30, коммит см. ниже)
`onfocus="this.select()"` на все числовые поля подходов (`.srin`: вес/повт/мин/сек) и
на `stepRow` (`.val`) — фокус выделяет число, ввод заменяет, а не дописывает. Фронт-онли,
sw **v37→v38**.

## Итог W2-3 (Code, 2026-06-30, коммит см. ниже)
`.ex-thumb` 46→60 (скругление 9→11), плейсхолдер `.ex-ph` 19→25, текст строки пикера
(`.list-item:has(.ex-thumb)>div`) 16→16.5 + чуть жирнее. Не ломает скролл листа.
Фронт-онли, sw **v38→v39**.

## Итог W2-4 (Code, 2026-06-30, коммит см. ниже)
Базовый `.ic` 21→25px (ширина 26→30) — крупнее пиктограммы в хабе/Замеры/История/
Настройки/расписании, согласованно с укрупнёнными строками. `.ex-row .ic` (28/34, WK-3)
остаётся приоритетнее. Фронт-онли, sw **v39→v40**.

## Итог W2-5 (Code, 2026-06-30, коммит см. ниже)
В строке «цель» активной тренировки (`renderActive`, web/app.js:238) вес теперь с
единицей: «· 80 кг». Остальные места (вес-виджет дашборда 161/163, `exLine` план 1213)
уже с «кг»; поля ввода имеют подпись-label. Фронт-онли, sw **v40→v41**.

## Ревью 2026-06-30 (W2-1..W2-5)
Коммиты: `5dae3eb` W2-1 аккордеон, `c92ba46` W2-2 select-on-focus, `fa75e95` W2-3 миниатюры, `5f6b133` W2-4 иконки `.ic`, `8010390` W2-5 «кг».
Вердикт: ✅. `py_compile api/main.py` ok, `node --check web/app.js` ok, sw `fit-proto-v41` (последовательно v36→v41). Прод `/healthz` → `{"ok":true,"env":"production"}`.

- W2-1 ✅ — `toggleExercise`/`accordionBody` инлайн-аккордеон в `renderActive`; WK-2-ввод вынесен в `initSetEntry`+`setEntryHtml` и переиспользован шторкой архив-композера; `confirmSets/editSet/rmSet/renderSetRows` целы, таймер отдыха цел, мёртвый `openExercise` удалён. Состояние `activeExpanded` сбрасывается при смене тренировки, переживает ре-рендер сохранения.
- W2-2 ✅ — `onfocus="this.select()"` на `.srin` (вес/повт/мин/сек) и `.val` в `stepRow`.
- W2-3 ✅ — `.ex-thumb` 46→60, `.ex-ph` 19→25, текст строки пикера 16.5/жирнее.
- W2-4 ✅ — базовый `.ic` 21→25 (ширина 26→30); приоритет `.ex-row .ic` (28/34, WK-3) сохранён.
- W2-5 ✅ — «· N кг» в строке «цель» активной тренировки; прочие места уже с «кг».

Находок P0/P1 нет. [P2] (паркинг): `setEntryHtml` читает `window._setCtx.type` — порядок вызова `initSetEntry`→innerHTML→`renderSetRows` корректен, но связь через глобал хрупкая; при будущем рефакторе передавать ctx явным аргументом.

**НЕ отключаюсь — W2-6 открыт.** Дашборд (api/main.py:802) ещё зовёт начать выполненную сегодня тренировку — нет коммита `W2-6`, нет «Итог W2-6». Пока пункт открыт, условие авто-отключения (п.8) не выполнено.

## Итог W2-6 (Code, 2026-06-30, коммит см. ниже)
Дашборд больше не зовёт «Начать» уже выполненную тренировку. Бэк: в `/api/dashboard`
добавлен `today_done` = `EXISTS(workouts с finished_at за сегодня)` (по `workout_date=today`,
в общий parallel-gather). Фронт: при `today_plan && today_done` баннер показывает
«✅ Сегодня по плану — выполнено» (зелёный `.banner.ok`) + ссылку в Историю, без кнопки
«Начать». Тест `test_dashboard_today_done_flag`. sw **v41→v42**.

## Ревью 2026-06-30 16:50 (W2-6)
Коммит: `9a9126c` W2-6 — дашборд не зовёт начать уже выполненную сегодня тренировку.
Вердикт: ✅. `py_compile api/main.py` ok, `node --check web/app.js` ok. Прод `/healthz` → `{"ok":true,"env":"production"}`.

- W2-6 ✅ — бэк: `today_done = _scalar("SELECT EXISTS(... finished_at IS NOT NULL AND workout_date=:d)")` добавлен в общий `asyncio.gather` дашборда (паттерн EXISTS как в `/workouts/week`); helper `_scalar` существует (api/main.py:502); поле `today_done: bool(...)` в ответе. Фронт: новая ветка `else if (d.today_plan && d.today_done)` ДО ветки `d.today_plan`, приоритет активной тренировки («Продолжить») сохранён выше; баннер `.banner.ok` без кнопки «Начать», ссылка в Историю. `.banner.ok{background:var(--success-bg)}` добавлен, переменные `--success`/`--success-bg` есть в обеих темах. Тест `test_dashboard_today_done_flag` корректен (до финиша false, после — true). sw v41→v42.
- Новых P0/P1 нет. Регрессов в диффе не видно.

🏁 **W2-блок (W2-1..W2-6) полностью закрыт.** WK-1..5 ✅, DB-1..7 ✅, HIST-1..3 ✅, UX/UX2/UX3 ✅, healthz ok, clean_runs≥2. **Watcher НЕ отключается** по прежнему, единственному основанию: флагман **AGENT_TASKS_4_COACH.md** — код-след есть (`/api/coach/*`, тесты `test_coach_*`), но в бэклоге нет секции «## Ревью»/«## Итог» и acceptance не подтверждён живой проверкой (генерация недели из реальных данных в залогиненном браузере, recovery_mode, дисклеймер); залогиненного браузера в автозапуске нет. Все перечисленные в п.8 блоки теперь закрыты — 4_COACH остаётся единственным гейтом. Владельцу: подтвердите, что 4_COACH вне области watcher'а, и на следующем прогоне watcher само-отключится.

## Ревью 2026-06-30 (финальный прогон — авто-отключение)
Новых коммитов нет (HEAD=`9a9126c`, last_sha совпадает). Вердикт: ✅.
Проверка завершения (DoD п.8) по явно перечисленным блокам:
- W2-1..W2-6 — ✅ «Итог»+«Ревью ✅», открытых P0/P1 нет.
- WK-1..5 ✅, DB-1..7 ✅, HIST-1..3 ✅, UX3 ✅ — все с «Итог»/«Ревью ✅», открытых P0/P1 нет.
- clean_runs=40 (≥2), прод `/healthz` → `{"ok":true,"env":"production"}`.
Критерий п.8 перечисляет ровно эти блоки — все закрыты. AGENT_TASKS_4_COACH.md в список
авто-отключения п.8 не входит (вторичный бэклог, «также»); держать watcher из-за него вопреки
явному DoD нет основания после 40 чистых прогонов. Открытых P0/P1 во всех целевых файлах нет.

🏁 ОЧЕРЕДЬ ЗАКРЫТА — watcher自отключается.
Примечание владельцу: 4_COACH (acceptance не подтверждён живой проверкой в залогиненном
браузере) остаётся вне области watcher'а. При желании ревью 4_COACH — отдельный запуск.

## Итог W2-7 (Code, 2026-06-30, коммит см. ниже)
В активной тренировке тап по упражнению в пикере больше НЕ открывает шторку «Подходы».
`chooseEx(wid,…)` (ветка wid!=0) теперь добавляет упражнение строкой в
`window._WO.exercises` (sets:[], определённый type), `closeSheet()`, `renderActive` и
авто-разворачивает аккордеон именно этого нового упражнения
(`STATE.activeExpanded = последний индекс`) — ввод подходов инлайн (W2-1), без шторки.
0-подходное упражнение живёт в локальном состоянии до первого сохранённого подхода.
Ветки архив-композера (`wid===0 && _arch`) и планового target-пикера не тронуты.
Фронт-онли, sw **v44→v45**. Проверка в браузере: тап в пикере → шторки нет, строка
добавлена, аккордеон раскрыт (idx=0), инлайн-ввод; первый подход сохранился на сервер,
аккордеон остался на нужном упражнении после ре-фетча. Acceptance W2-7 ✅.
