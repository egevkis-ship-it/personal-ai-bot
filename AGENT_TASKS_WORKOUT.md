# Бриф WORKOUT — ввод подходов и таймер отдыха (находки владельца, живой проход)

Правила те же: логика в `app/`/`api`, веб тонкий, owner-scoped, `esc()`, секреты не
вписывать, обратная совместимость, `py_compile`+`node --check`, sw +1 при изменении
фронта, РЕАЛЬНАЯ проверка в браузере, push → деплой `bovve9287cs7jjhkteh2bdfk`. Один
коммит на блок.

## WK-1 — Активная тренировка не уважает выключенный таймер (баг)
Точный сценарий владельца: таймер был включён; в середине тренировки он **выключил** тумблер
в Настройках, вернулся в тренировку, **завершил подход — и таймер автозапустился снова**, как
будто он всё ещё включён. То есть выключение настройки НЕ применяется к уже идущей сессии:
автостарт отдыха на завершении подхода (`web/app.js:377-378`: `if (st.rest_timer_enabled !==
false) restTimer(...)`) всё равно срабатывает.
- Вероятная причина: активная тренировка использует **устаревший снимок** настроек —
  `window._SETTINGS` не обновился после правки в Настройках (или захвачен в замыкании на
  старте сессии). Хотя `saveRestTimer()` (`:1730`) пишет `_SETTINGS.rest_timer_enabled`,
  путь сохранения подхода читает не его, либо `Active()` (`:218`) переиспользует старый кэш.
- Фикс: активная тренировка должна читать **актуальную** настройку таймера в момент
  завершения подхода. Гарантировать единый источник правды (`window._SETTINGS` обновляется
  и в Настройках, и при возврате в тренировку), чтобы при выключенном таймере подход НЕ
  запускал отсчёт. Дополнительно: при выключении в Настройках погасить уже идущий отсчёт
  (`stopTimer()`).
- Acceptance: выключил таймер в Настройках → вернулся → завершение подхода НЕ запускает
  отсчёт; включил обратно → запускает. Любой идущий отсчёт гаснет сразу при выключении.

## WK-2 — Гибкий ввод подходов: несколько полей сразу + голос/текст (как в боте)
Сейчас тап по упражнению в активной тренировке открывает шторку на **один** подход, а
число подходов жёстко задано плановой программой (`target_sets`). Владелец хочет вводить
один подход, несколько или **сразу все**, как было в боте.

Переделать шторку добавления подходов (`addSet`/`sheet`, район `web/app.js:325-378`):
1. **Мультиввод структурно:** показывать СРАЗУ несколько строк «вес × повторы» (по
   умолчанию количество = `target_sets` упражнения, напр. 4; если плана нет — 1–3).
   Каждая строка редактируется; есть **«➕ добавить ещё подход»** и удаление лишней строки.
   Префилл разумными значениями (последний/плановый вес и повторы), но всё правится.
   Кнопка сохранения вносит ВСЕ заполненные строки как подходы разом (НЕ ограничиваться
   плановым числом — можно меньше или больше).
2. **Голос/текст внизу как опция:** поле свободного ввода + микрофон (как в боте). Текст
   вида «80×10, 82×8, 80×8» парсится в НЕСКОЛЬКО подходов за раз. Бэкенд уже готов:
   `POST /api/workouts/{id}/sets` с `{text}` прогоняет тот же парсер (`parse_exercise_input`,
   `api/main.py:1188`) и вставляет все распознанные подходы; голос — через
   `POST /api/voice/transcribe` → затем тем же текстовым путём. Реиспользовать, не
   дублировать логику.
3. Для упражнений на время/кардио (`exercise_type`=time/bodyweight) — поля адаптировать
   (время/повторы без веса), как сейчас, но в том же мультивводе.
4. После сохранения — корректно отрабатывает таймер отдыха (WK-1) и обновление списка
   подходов упражнения.
- Acceptance: из шторки можно добавить один, несколько или все подходы разом структурными
  полями; есть «добавить ещё»; есть голос/текст, парсящий несколько подходов; не привязано
  жёстко к плановому числу; типы упражнений (вес/время) корректны; таймер ведёт себя по WK-1.

> Связано с UX3-FIX-5 (высота шторки/скролл) — мультиввод не должен ломать закрытие листа.

## WK-3 — Строки упражнений крупнее (пиктограмма + текст + отступы, пропорционально)
Статус-иконки упражнения (`.ic`: ✅ выполнено / ▶️ текущее / ⚪️ ожидает / 💤 отдых) мелковаты.
Но увеличивать надо НЕ только эмодзи — иначе строка станет несбалансированной (иконка-переросток
рядом с мелким текстом). Поднять **всю строку упражнения как единый блок** на ~30-40%:
- **иконка `.ic`** (+~35%), **название упражнения** (крупнее/жирнее), **подпись** (подходы/цель —
  тоже чуть крупнее), **высота строки и внутренние отступы** — пропорционально, чтобы строка
  смотрелась крупнее, читаемее и сбалансированно.
- Применить в: активной тренировке (`renderActive`, `web/app.js:236-239`) и в просмотре плана
  (PlanView) / списках дня, где те же строки упражнений.
- Сделать через отдельный модификатор-класс на этих строках (напр. `.list-item.ex-row`), НЕ
  раздувая глобально все `.ic`/`.list-item` (хаб-меню, настройки и пр. оставить как есть).
- Acceptance: в планах и активной тренировке строки упражнений заметно крупнее и читаемее
  (иконка+текст+отступы согласованы); остальные экраны/меню не поехали.

## WK-4 — Настройка таймера должна сохраняться для пользователя (персистентно)
Владелец: таймер «судя по всему включается каждый раз по умолчанию» — настройка не держится
между сессиями. Бэкенд её ХРАНИТ и отдаёт: `GET /api/settings` возвращает `rest_timer_enabled`
(дефолт `true`, если в БД NULL), `PATCH` пишет `bool` (`api/main.py:771-800`). Значит проблема
на фронте: активная тренировка/таймер должны опираться на **persisted-значение как единый
источник правды**, а не включаться по дефолту каждую сессию.
- Фикс: на старте приложения `window._SETTINGS` грузится из `/settings` и `rest_timer_enabled`
  читается именно оттуда везде (Настройки, активная тренировка, автозапуск отдыха). Тумблер в
  Настройках сохраняет (`PATCH`) и обновляет `_SETTINGS` — убедиться, что после перезахода
  значение приходит с сервера и применяется (нет жёсткого дефолта ON в обход сохранённого).
  Свериться с WK-1, чтобы не было второго «сессионного» флага, перетирающего persisted.
- Acceptance: выключил таймер → перезашёл/новая тренировка на след. день → таймер ВЫКЛЮЧЕН
  (как сохранено); включил → включён. Дефолт ON только для пользователя, который НИКОГДА не
  трогал настройку.

## WK-5 — Таймер дёргается каждую секунду (анимация)
Обратный отсчёт визуально «дёргается», будто перезапускается каждую секунду. Причина в коде:
`restTimer` (`web/app.js:509-518`) каждую секунду пересобирает ВЕСЬ лист
(`bg.innerHTML = '<div class="sheet">…'`) внутри `setInterval`, пересоздавая DOM (и сбрасывая
любые CSS-переходы) на каждом тике.
- Фикс: строить лист ОДИН раз, а в интервале обновлять только текстовый узел с цифрой
  (`textContent`) и, если есть кольцо/полоса прогресса — анимировать его CSS-переходом
  (`stroke-dashoffset`/`transform` с `transition`), не пересоздавая элемент. Тогда отсчёт идёт
  плавно, без скачков.
- Acceptance: цифра меняется плавно, лист/кольцо не моргают и не «прыгают» каждую секунду.

## Итог WK-1 (Code, 2026-06-29, коммит `e0b9df7`)
Причина бага: iOS-свитч таймера в Настройках только переключал визуально
(`onclick="this.classList.toggle('on')"`) — чтобы применить, нужно было нажать
«Сохранить таймер». Поэтому активная сессия читала устаревший `window._SETTINGS`.
Фикс: свитч применяется сразу — новый `toggleRestTimer(el)` обновляет единый
источник правды `window._SETTINGS`, шлёт `PATCH /settings` и при ВЫКЛ гасит идущий
отсчёт (`stopTimer()`+`closeRest()`). `confirmSet` уже читает `window._SETTINGS` →
завершение подхода уважает живую настройку. Фронт-онли, sw **v27→v28**. Проверка в
браузере: ВЫКЛ → вернулся → завершение подхода НЕ стартует таймер; ВКЛ → стартует;
идущий отсчёт гаснет мгновенно при ВЫКЛ; настройка персистится. Acceptance WK-1 ✅.

## Итог WK-2 (Code, 2026-06-29, коммит `3bc342d`)
Шторка добавления подходов переделана в мультиввод.
- **Структурно (основной путь, полностью проверен в браузере):** открывается с
  `target_sets` префилл-строками (нет плана → 3, время → 1), каждая «вес × повторы»
  правится; «➕ Добавить ещё подход» и удаление строки ✕ (add/remove сохраняют
  введённое); на строку — тумблер разминки «Р». «✓ Сохранить (N)» вносит ВСЕ
  заполненные строки разом (пустые пропускаются), НЕ ограничено планом. Поля по типу:
  сила (вес×повт), своим весом (повт), время (мин:сек + живой таймер, который по
  стопу добавляет строку с засечённым временем).
- **Голос/текст внизу:** свободный ввод + микрофон, привязан к текущему упражнению
  (передаём `exercise_name` как подсказку парсеру).
- **Бэкенд (тонко):** `AddSet.sets: list[dict]` — несколько строк в ОДНОЙ
  идемпотентной транзакции (разминка по строке, пустые скип, 422 если всё пусто).
  Текстовый путь теперь как в боте: regex-парсер, при многоподходном разделителе
  («,», «;», перевод строки, «и») или пустом результате — AI-парсер
  `parse_set_text_ai`, поэтому «80×10, 82×8, 80×8» даёт несколько подходов. Если AI
  недоступен — мягкая деградация (оставляет результат regex).
- **Проверка:** структурный путь — реально в браузере (4 строки по плану, префилл,
  add/remove с сохранением значений, разминка по строке, сохранение 3 из 4 при одной
  пустой, типы сила/вес/время, таймер по WK-1 вкл/выкл, лист закрывается — UX3-FIX-5
  цел). Текст: «55 10 60 8 55 8» → 3 подхода (regex+контекст, без AI); «32x10, 34x8»
  на dev (AI выключен) → мягко 1 подход без ошибки; на проде AI=ok, даст несколько.
  pytest 135 (+1 `test_set_batch_structured_rows`). sw **v28→v29**. Прод healthz ok,
  /app.js обновлён, sw v29. Запятая/голос на проде — финальный смоук на твоём устройстве.
  Acceptance WK-2 ✅ (структура), голос/запятая-AI — на проде живой.

## WK-3 — строки упражнений крупнее (пропорционально)
Увеличить не только эмодзи, а всю строку упражнения как единый блок на ~30-40%:
иконка `.ic` (+~35%), название (крупнее/жирнее), подпись (подходы/цель), высота
строки и отступы — согласованно. Применить в активной тренировке (`renderActive`,
`web/app.js:236-239`) и в просмотре плана / списках дня. Через ОТДЕЛЬНЫЙ
модификатор-класс (`.list-item.ex-row`), НЕ раздувая глобально `.ic`/`.list-item`
(хаб-меню, настройки оставить как есть). Acceptance: строки заметно крупнее и
читаемее, остальные экраны не поехали.

## WK-4 — настройка таймера персистентна
Сейчас «включается по умолчанию каждый раз». Бэкенд хранит и отдаёт
(`GET /api/settings` → `rest_timer_enabled`, дефолт `true` при NULL; `PATCH` пишет
bool). На фронте сделать persisted-значение единым источником правды: на старте
грузить из `/settings`, читать везде (Настройки, активная тренировка, автозапуск
отдыха), не сбрасывать в ON каждую сессию. Свериться с WK-1, чтобы не остался
второй «сессионный» флаг, перетирающий сохранённое. Acceptance: выключил →
перезашёл/новый день → выключен; включил → включён.

## WK-5 — дёрганье таймера
`restTimer` (`web/app.js:509-518`) каждую секунду пересобирает весь лист
(`bg.innerHTML = …`) в `setInterval`, пересоздавая DOM и сбрасывая CSS-переходы →
скачки. Фикс: строить лист один раз, в интервале обновлять только цифру
(`textContent`) и кольцо прогресса (если есть) — CSS-переходом, не пересоздавая
элемент. Acceptance: цифра меняется плавно, лист не моргает.

## Итог WK-3 (Code, 2026-06-30, коммит `a09ce67`)
Строка упражнения увеличена как единый блок ~35% через ОТДЕЛЬНЫЙ модификатор
`.ex-row`, чтобы выросли только строки упражнений/тренировок, а хаб-меню и настройки
остались компактными. CSS: `.ex-row` → `.ic` 21→28px (ширина 26→34), название `<b>`
16→18px, `.small` 13→15px; `.list-item.ex-row` падинг 13→17px + gap 12→14;
`.card.ex-row` падинг 17px. Класс добавлен ТОЛЬКО на три view-поверхности: активная
тренировка (`renderActive`), строки тренировок недели (`ChooseDay`), карточки
упражнений в просмотре плана (`PlanView`). НЕ тронуты: train-хаб, меню тренировки,
контроль доступа, пикеры. Фронт-онли, sw **v29→v30**. Проверка в браузере: в
`.ex-row` иконка=28px/имя=18px/подпись=15px/падинг=17px; строки хаб-меню остались
21px/16px/13px/13px. pytest 135 зелёный. Acceptance WK-3 ✅.

## Итог WK-4 (Code, 2026-06-30, коммит `e557b2a`)
Проверка показала: корневая причина «включается каждый раз» уже устранена в WK-1 —
раньше свитч переключался только визуально и не сохранялся без «Сохранить таймер»;
теперь `toggleRestTimer` шлёт `PATCH` сразу. End-to-end проверено: выключил → свежий
boot → `window._SETTINGS=false`, сервер=false, свитч в Настройках off, автозапуск НЕ
срабатывает. Второго «сессионного» флага нет — единственное состояние `window._SETTINGS`.
Чтобы закрепить и захардить от регрессий:
- Новые хелперы `restEnabled()`/`restSecs()` — ЕДИНСТВЕННАЯ точка чтения настройки
  таймера (автозапуск после подхода + кнопка «Таймер отдыха»), вместо разбросанных
  проверок `_SETTINGS.rest_timer_enabled !== false`.
- `window._SETTINGS` зеркалится в localStorage (`settings_v1`) и восстанавливается при
  упавшем boot/Active-фетче → выключенный таймер переживает ОФФЛАЙН / новую сессию, а
  не сбрасывается в ON. При полном отсутствии состояния — дефолт ON (как NULL→true на
  бэке).
- Каждый вход в Настройки синхронит `window._SETTINGS` из свежего `/settings`
  (актуальность с сервером/ботом/другими устройствами).
Фронт-онли, sw **v30→v31**. Проверка в браузере: выключение переживает boot и оффлайн
(кэш), включение распространяется, синк Настроек работает, автозапуск слушает живое
сохранённое значение. pytest 135. Acceptance WK-4 ✅.

## Итог WK-5 (Code, 2026-06-30, коммит `f67c325`)
`restTimer` пересобирал весь лист через `bg.innerHTML` каждую секунду в интервале —
пересоздавал DOM и сбрасывал CSS-переходы, отсюда скачки/моргание.
Фикс: лист строится ОДИН раз; в интервале меняются только `numEl.textContent` и
`stroke-dashoffset` кольца. Добавлено SVG-кольцо прогресса (r=54), анимируется через
`transition: stroke-dashoffset 1s linear` → кольцо плавно убывает, цифра тикает без
 re-render. `restTimer()` сначала зовёт `closeRest()` (нельзя сложить два оверлея);
«+30» расширяет и остаток, и total, чтобы кольцо оставалось пропорциональным.
Фронт-онли, sw **v31→v32**. Проверка в браузере: ноды `.sheet` и цифры переживают
тики (без пересборки), цифра 0:30→0:29, offset кольца анимируется (0→11.31, переход
1s linear), «+30» поднимает цифру и не пересоздаёт ноду. pytest 135. Acceptance WK-5 ✅.
