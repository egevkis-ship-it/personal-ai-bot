# Бриф PARSE — корректное распознавание детальных программ (по коммиту на блок)

Правила: логика в `app/`/`api`, веб тонкий, owner-scoped на всех `{id}`, `esc()`, секреты не
вписывать, обратная совместимость, `py_compile`+`node --check`, sw +1 при изменении фронта,
РЕАЛЬНАЯ проверка в браузере, push → деплой `bovve9287cs7jjhkteh2bdfk` → `/healthz` ok.
Один коммит на логический блок, итог — в секцию после блока.

> **Каталог `data/exercise_catalog_v2.json` — файл РЕПОЗИТОРИЯ, не прод-база.** Его правим свободно
> (деплоем). Прод-базу (workouts/plans/exercise_sets) руками НЕ трогаем.
>
> **Главный принцип именования (подтверждён владельцем, не нарушать):** распознавание расширяем,
> но БЕЗ молчаливых подмен. Что не легло точно в канон — идёт в диалог подтверждения (DB-7:
> похоже / оставить как есть / создать новое). Разные по механике движения НЕ сливать в одно.

## Контекст: эталонная программа (фикстура для приёмки)
Владелец вставляет недельные планы такого вида (даты будущие, «Комментарий:», диапазоны, ярусы):
```
2026-07-10 — Пятница. Плечи приоритет + руки
2. Махи гантелями в стороны — 2×12 на 12.5 кг + 2×15 на 10 кг. Комментарий: тяжёлые в начале, пампинг в конце.
3. Махи одной рукой в кроссовере — 1×10-12 на 15 кг + 3×10-12 на 10 кг.
...
2026-07-11 — Суббота. Верх груди + ягодицы 2-й день
1. Наклонный жим штанги 30° — 4×10-12, 75 кг. Комментарий: цель — 4×12, минимум 10. Если грязно — откат на 72.5.
2. Сведение в кроссовере снизу-вверх — 4×12, 20 кг на руку.
4. Frog pump — 3×15-20, без веса.
```
Полный текст программы — в чате (2026-07-06 … 2026-07-12). Приёмка каждого блока — прогнать эту
программу через `/api/plans/parse` на проде и проверить конкретный аспект блока.

## Диагноз (что сейчас ломается на этой программе)
1. **Имена с квалификаторами** («…30°», «…20°», «(кикбэк)», «снизу-вверх», «с упором двумя руками»,
   «стоя», «узким хватом», склонения «гантелей→гантелями») не находят точного канона → лишние
   подтверждения. В каталоге есть упрощённые формы («Жим штанги на наклонной», «Отведение ноги в
   кроссовере», «Тяга Т-грифа», «Подъём гантелей на бицепс», «Сведение в кроссовере»…).
2. **Нет в каталоге вовсе:** «Frog pump». Возможны и другие после аудита.
3. **Ярусные веса** «2×12 на 12.5 + 2×15 на 10», «1×10-12 на 15 + 3×10-12 на 10» — модель плана
   держит ОДИН вес на упражнение → второй ярус теряется. **Владелец выбрал: поддержать структурно.**
4. **Кардио/разминка в плане** «велотренажёр 7-10 мин» — у планового упражнения нет длительности.
5. **Контекст подхода** «на руку/каждой/одной ногой», «компенсация 50 кг» (гравитрон).

---

## REC-1 — Каталог: синонимы для реальных формулировок + недостающие упражнения
Расширить `data/exercise_catalog_v2.json`.
- **Синонимы (aliases) к СУЩЕСТВУЮЩИМ канонам** — для чисто словесных/квалификаторных вариантов
  (угол, хват, стойка, сторона, склонение). Как минимум покрыть из эталона:
  - «Наклонный жим штанги», «Жим штанги 30°», «Жим штанги под углом» → **Жим штанги на наклонной**
  - «Жим гантелей под углом», «Жим гантелей 20°», «Наклонный жим гантелей» → **Жим гантелей на наклонной**
  - «Отжимания на брусьях узким хватом» → **Отжимания на брусьях**
  - «Подъём гантелей на бицепс стоя» → **Подъём гантелей на бицепс**
  - «Сведение в кроссовере снизу-вверх», «Сведение рук в кроссовере» → **Сведение в кроссовере**
  - «Махи гантелей в стороны» (склонение) → **Махи гантелями в стороны**
  - «Отведение ноги в кроссовере (кикбэк)», «Кикбэк», «Ягодичный кикбэк» → **Отведение ноги в кроссовере**
- **Недостающие — добавить НОВЫМИ записями** с корректным `input` (strength/bodyweight/time),
  `muscle_group` (14-групповая таксономия), `aliases`, `image` (если есть — иначе null/плейсхолдер):
  - **Frog pump** / «Фрог-памп» — ягодицы, `input: bodyweight` (повторы, вес опционально).
  - прочие, всплывшие при аудите каталога против эталона.
- **НЕ сливать разное (критично — на этом уже обжигались).** Варианты с реально другой механикой/
  оборудованием оставить ОТДЕЛЬНЫМИ записями, а не алиасить в базовое. Пограничные из эталона —
  твоё решение по аудиту, НО:
  - «Тяга Т-грифа с упором» (с упором в грудь) ≠ «Тяга Т-грифа» (лендмайн) — если считаешь разными,
    заведи новую запись, не алиась.
  - «Рычажная тяга с упором двумя руками» vs существующая «Рычажная тяга с упором» — проверь, одно
    ли это; если да — алиас, если нет — новая запись.
- **Отчётность:** в итог блока приложи ТАБЛИЦУ решений: `формула из эталона → [алиас к X / новая
  запись / оставлено на диалог]`, чтобы владелец мог пробежать глазами. Ничего не сливать «молча».
- Acceptance: прогон эталона через `/api/plans/parse` — доля `resolved:true` заметно выросла;
  «Frog pump» распознан; ни одно РАЗНОЕ движение не склеено; спорное — помечено `resolved:false`
  (уйдёт в диалог), а не подставлено неверно.

## REC-3 — Скормить каталог AI-парсеру (маппинг только при уверенности)  [приоритет]
Владелец выбрал: дать парсеру список канонических имён, чтобы он сам сводил формулировки к
существующим — меньше ручных подтверждений.
- В `_PLAN_SYSTEM` и `_LOG_SYSTEM` (`app/bot/services/ai_parser.py`) добавить компактный справочник
  канонических имён каталога (≈187 строк — дёшево). Инструкция модели: «Вот канонические названия из
  базы. Если упражнение ЯВНО одно из них (отличается только углом/хватом/стойкой/стороной/склонением)
  — верни ТОЧНОЕ каноническое имя из списка. Если не уверен или движения нет в списке — верни имя как
  в тексте, НЕ выдумывай и НЕ подгоняй под похожее».
- Справочник строить из `CATALOG` в рантайме (не хардкод), чтобы он не устаревал при правках REC-1.
- Инвариант: неизвестное упражнение обязано пройти вербатим → `_resolve_name` вернёт `resolved:false`
  → диалог DB-7. Парсер НЕ заменяет на «похожее».
- Acceptance: «Наклонный жим штанги 30°» → парсер вернул «Жим штанги на наклонной» (resolved:true);
  выдуманное/незнакомое — осталось как есть (resolved:false), молчаливой подмены нет.

## REC-4 — Ярусные веса: структурная поддержка нескольких весовых ярусов на упражнение
«2×12 на 12.5 + 2×15 на 10» = два яруса (sets×reps×weight). Сейчас теряется. Сделать структурно,
с обратной совместимостью.
- **Модель** `PlanExercise` (`api/main.py:2019`): добавить `set_groups: Optional[list[SetGroup]]`,
  где `SetGroup = {sets:int, reps_min:int?, reps_max:int?, weight:float?, reps_text:str?}`.
  - Если `set_groups` пуст/None → поведение как сейчас (плоские `target_*`).
  - Если задан → он источник истины; плоские `target_*` заполнять из ПЕРВОГО яруса (чтобы старые
    читатели/сводки/`_pe` не падали).
- **Хранение**: колонка `set_groups JSONB` (nullable) на `planned_exercises`. Добавить идемпотентно
  на старте API (`api/schema.py`, как делали `workout_exercise_notes`) + миграционный файл в
  `app/db/migrations/`. `_clean_plan_exercises`/`_normalize_plan_exercises`/`create_planned_workout`
  — прокинуть `set_groups` (валидация: sets≥1, веса/повторы в разумных границах).
- **Парсер** `_PLAN_SYSTEM`: научить эмитить `set_groups`, когда строка кодирует несколько ярусов
  («A×R на W + B×R2 на W2», «A×R×W, потом B…», пирамида/«тяжёлые+пампинг»). Одноярусные строки —
  плоские `target_*` как раньше (не плодить `set_groups` без нужды). Обновить пример в промпте.
- **Превью/сохранение/чтение**: `/api/plans/parse`, `/api/plans/bulk`, `/api/plans`, `/api/plans/{pid}`
  — возить `set_groups`.
- **Фронт**: в просмотре/редакторе плана рисовать ярусы («2×12 · 12.5 кг + 2×15 · 10 кг»); базовое
  редактирование ярусов. sw +1.
- **План → тренировка**: при старте тренировки из плана разворачивать ярусы в подходы (2 подхода
  12.5×12 + 2 подхода 10×15), а не один. Точка — `_pe`/сидинг подходов из плана (`api/main.py:538`).
- Acceptance: эталон Пт №2/№3 в превью показывает ОБА яруса; после сохранения план хранит оба;
  старт тренировки из плана создаёт подходы по обоим ярусам; одноярусные упражнения не изменились.

## REC-5 — Кардио/разминка в плане: длительность вместо «7×10»
«Разминка — велотренажёр 7-10 мин»: у планового упражнения нет времени.
- `PlanExercise`: добавить `target_duration_seconds: Optional[int]` (+ при желании min/max). Для
  `input:time`-упражнений парсер кладёт время, `target_reps_*`=null.
- Хранение (колонка/поле как у REC-4-подхода), превью/сохранение/чтение, рендер «7–10 мин» вместо
  подходов×повторов. Дефолты при добавлении time-упражнения в план — минуты, не «N×M».
- Acceptance: велотренажёр/разминка в плане показывается как минуты; кардио не предлагает вес×повторы.

## REC-6 — Контекстные квалификаторы подхода
- «на руку», «каждой», «одной ногой», «на ногу» → число веса/повторов НЕ портится; квалификатор —
  в заметку упражнения. (Парсер плана и `set_parser.py`.)
- «компенсация N кг» / «противовес N» (гравитрон, ассистированные) → `weight = N` (это нагрузка
  противовеса, так и логируется — W3-1), плюс слово в заметку.
- Acceptance: «20 кг на руку» → вес 20 + заметка «на руку»; «компенсация 50 кг» → вес 50.

## REC-2 — Безопасная нормализация имён на резолве (страховка, без молчаливого fuzzy)  [P2]
Дополнительно к REC-1/REC-3 — детерминированная нормализация ПЕРЕД точным матчем (помогает не-AI
путям: быстрый текст-ввод подхода, лог). Строго: только чтобы найти ТОЧНУЮ запись каталога.
- lower, ё→е, схлопнуть пробелы, срезать хвостовые скобки «(…)», срезать белый список хвостовых
  квалификаторов (`\d+°`, «под углом \d+», «узким/широким/обратным хватом», хвостовые «стоя/сидя/
  лёжа», «одной рукой/ногой», «на руку/ногу», «каждой») → затем ТОЧНЫЙ матч в NAME_INDEX.
- Если после нормализации точного совпадения нет → диалог DB-7. НИКОГДА не маппить на «похожее»
  другого стема/группы. Консервативно: сомневаешься — в диалог.
- Acceptance: «Жим штанги 30°» резолвится точно; «Жим стоя» (несуществующее) НЕ схлопывается в
  случайный «жим», а уходит в диалог.

---

# Итоги
<!-- Code дописывает ## Итог REC-1 / REC-3 / REC-4 / REC-5 / REC-6 / REC-2 после каждого коммита -->

# Итоги

## Итог REC-1 (Code, 2026-07-05)
Каталог расширен: +9 алиасов к существующим канонам и +1 новая запись. Каталог 187→188.
Ничего разного не слито; сомнительное — помечено ниже для владельца, а не подставлено молча.

**Таблица решений (формула из эталона → решение):**

| Формула из эталона | Решение | Канон |
|---|---|---|
| Наклонный жим штанги / Жим штанги 30° / под углом | алиас | Жим штанги на наклонной |
| Жим гантелей под углом / 20° / Наклонный жим гантелей | алиас | Жим гантелей на наклонной |
| Отжимания на брусьях узким хватом | алиас¹ | Отжимания на брусьях |
| Подъём гантелей на бицепс стоя | алиас | Подъём гантелей на бицепс |
| Сведение в кроссовере снизу-вверх / Сведение рук в кроссовере | алиас | Сведение в кроссовере |
| Махи гантелей в стороны (склонение) | алиас | Махи гантелями в стороны |
| Отведение ноги в кроссовере (кикбэк) / Кикбэк / Ягодичный кикбэк | алиас² | Отведение ноги в кроссовере |
| Рычажная тяга с упором двумя руками | алиас | Рычажная тяга с упором |
| Frog pump / Фрог-памп / фрогпамп | **новая запись** | Frog pump (glutes, bodyweight) |
| Тяга Т-грифа с упором в грудь | оставлено как есть³ | (уже алиас «Тяга Т-грифа») |

¹ Узкий хват на брусьях смещает акцент в трицепс, но владелец трактует как тот же брусья-паттерн
(в каталоге есть отдельная «Отжимания от скамьи на трицепс»/«брусья узко» — не тронута). **На ревью.**
² «Кикбэк»/«ягодичный кикбэк» → кроссоверный вариант (по указанию владельца). Отдельная «Махи ногой
назад (ягодицы)» (`glute_kickback`) оставлена самостоятельной записью. **На ревью:** «ягодичный
кикбэк» пограничен (мог бы быть bodyweight-версией).
³ «с упором в грудь» уже алиас лендмайн-«Тяга Т-грифа». Если это отдельный тренажёр — скажи,
расклею как «Махи в кроссовере» в W4-7.

**НЕ слито (осознанно):** «Махи в кроссовере» ≠ «Махи гантелями в стороны» (W4-7 сохранён);
кроссоверный кикбэк ≠ «Махи ногой назад».
Данные-онли (sw без бампа). Проверка: все 15 форм из эталона резолвятся в верный канон
(детерминированно, `name_to_key`); «Frog pump» распознан (glutes/bodyweight); тесты каталога (82)
зелёные. Полная приёмка `/api/plans/parse` (с AI) — на проде после REC-3. Acceptance REC-1 ✅.
