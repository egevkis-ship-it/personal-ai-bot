# Бриф HISTORY2 — структурировать экран Истории (по коммиту на блок)

Правила: веб тонкий, `node --check`, sw +1, проверка в браузере, push → деплой
`bovve9287cs7jjhkteh2bdfk` → `/healthz` ok, итог в секцию после блока.

## HIST2-1 — группировка списка по месяцам/неделям (основное)
`History()` сейчас — плоский список карточек по дате. Сгруппировать завершённые:
- Месяц — sticky-заголовок «Июнь 2026» + сводка справа (число тренировок + суммарный
  тоннаж за месяц);
- внутри месяца — тонкие под-разделители по неделям (Пн–Вс в tz юзера): «23–29 июня»
  + число тренировок недели;
- карточки — как сейчас (день недели · дата · подходы · тоннаж), date-desc, новейший
  месяц сверху;
- при активном поиске — плоский отфильтрованный список без секций;
- «📄 Отчёты (PDF)» и «➕ Добавить тренировку» в шапке остаются.
Фронт-онли: группировать `/api/workouts` на клиенте; суммы из `tonnage`/`set_count`.

## HIST2-2 — переключатель Список / Календарь (опционально)
«Календарь» = месячная сетка (общий `monthCalendar`) с точками на днях с тренировкой,
тап по дню → тренировка(и) дня. Зеркало «Расписания» (план — вперёд, история — назад).
Переиспользовать `monthCalendar`.

## Итог HIST2-1 (Code, 2026-06-30, коммит `3b54989`)
`History()` группирует завершённые тренировки на клиенте: sticky-заголовок месяца
(«Июнь 2026» + N трен · суммарный тоннаж, новейший сверху), внутри — тонкие
недельные под-разделители «23–29 июня · N» (Пн–Вс; подпись через месяц при переходе
границы, напр. «29 июня – 5 июля»), карточки как раньше (date-desc). Поиск → плоский
список без секций. Суммы из `tonnage`/`set_count`. Вынес `histCard()`, добавил
`groupHistory()` + хелперы меток; CSS `.hist-month-h`(sticky)/`.hist-week-h`.
Фронт-онли, sw **v42→v43**. Проверка в браузере: месяцы/недели с корректными счётом+
тоннажем, sticky, кросс-месячные недели; поиск → плоско. Acceptance HIST2-1 ✅.

## Итог HIST2-2 (Code, 2026-06-30, коммит см. ниже)
Переключатель «Список / Календарь» (segmented) в шапке Истории. «Календарь» — общий
`monthCalendar` с точками на днях с тренировкой; тап по дню → его тренировки под
сеткой (зеркало «Расписания», только назад). `histCalendar/histSetView/histCalNav/
histCalPick`; поиск в режиме календаря скрыт. Переиспользован `monthCalendar`, без
дублирования. Фронт-онли, sw **v43→v44**. Проверка в браузере: тумблер, точки на 2
днях, скрытый поиск, тап дня показывает тренировку + подпись дня, возврат в Список
восстанавливает группировку. Acceptance HIST2-2 ✅.
